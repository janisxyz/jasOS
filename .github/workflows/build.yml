name: build

on:
  push:
  pull_request:

jobs:
  host:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: host selftest
        run: make host
      - name: kernel.elf
        run: make kernel
